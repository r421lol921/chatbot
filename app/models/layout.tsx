export const metadata = {
  title: "Models - Lio",
  description: "Explore Lio AI models",
};

export default function ModelsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
