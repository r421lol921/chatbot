import type { ReactNode } from "react";

export const metadata = {
  title: "Library — PeytOtoria",
  description: "Your saved AI-generated content",
};

export default function LibraryLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
